type User {
  id: Int!
  username: String!
  messages: [Message!]!
}

type Query {
  users: [User!]!
  user(id: Int!): User
}

type Mutation {
  createUser(username: String!): User!
}

type Subscription {
  userAdded: User!
}
